# Generated by awx_vcenter_lab. Contains no vCenter credentials.
{% for group_name in awx_vcenter_inventory_groups %}
[{{ group_name }}]
{% for vm in awx_vcenter_lab_vms %}
{{ vm.name }} ansible_host={{ vm.ip | default('') }}{% if awx_vcenter_ansible_user | length > 0 %} ansible_user={{ awx_vcenter_ansible_user }}{% endif %}{% if awx_vcenter_ansible_ssh_private_key_file | length > 0 %} ansible_ssh_private_key_file={{ awx_vcenter_ansible_ssh_private_key_file }}{% endif %}
{% endfor %}

{% endfor %}
[awx_server_nodes:children]
{% for group_name in awx_vcenter_inventory_groups %}
{{ group_name }}
{% endfor %}
