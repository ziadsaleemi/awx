# AWX UI Enhancements

Planned and in-progress UI/UX improvements for this AWX fork.

Legend: ⬜ Not started · 🔄 In progress · ✅ Done

---

## Already Completed

| # | Enhancement | Notes |
|---|-------------|-------|
| ✅ | Live system usage bar in navbar | Polls `/api/v2/instances/` every 30s; shows jobs count + progress bar + % with colour coding |
| ✅ | Usage bar moved to right side of navbar | Positioned after notifications, before help menu |
| ✅ | Overview page full-width on large screens | Removed `isWidthLimited` from `PageSection` in `PageDashboard.tsx` |
| ✅ | Dashboard card side padding removed | `PageSection` set to `padding: '16px 0'` so cards touch the content edges |
| ✅ | Counts card full-width | Changed `width="xxl"` → `width="full"` in `PageDashboardCountBar` |
| ✅ | Job Activity card full-width | Changed `width="xxl"` → `width="full"` in `AwxJobActivityCard` |
| ✅ | Jobs + Projects cards half-width each | `width="half"` — side-by-side on large screens |
| ✅ | Inventories card full-width | `width="full"` |
| ✅ | Resume workflow job support | `useResumeWorkflowJob` hook added |
| ✅ | Custom logo + login page branding | Logo upload via Settings, shown on login and masthead |
| ✅ | Custom settings navigation | Reorganised settings sidebar |
| ✅ | About page version + enhanced-by credit | Version `25.0.0` shown in About modal; "Enhanced by [Ziad Saleemi](https://ziadsaleemi.com)" link added |
| ✅ | Terraform always runs in container | `hashicorp/terraform:latest` used as default EE; auto-detects `docker`/`podman`; AWX node no longer needs Terraform installed |

---

## Planned Enhancements

### Dashboard & Navigation

| # | Enhancement | Priority | Status |
|---|-------------|----------|--------|
| 1 | **Collapsible sidebar** — icon-only mode to save horizontal space on smaller screens | Medium | ✅ |
| 2 | **Dashboard card drag-to-reorder** — persist custom card order per user | Low | ✅ |
| 3 | **Dark/light mode persistence** — store theme and user UI preferences in the AWX user profile (`/api/v2/me/`) with localStorage fallback for anonymous/offline states, so preference changes survive browser changes | Medium | ✅ |
| 4 | **Job Activity card drill-down** — clicking a date point navigates to the jobs list filtered to that day | Medium | ✅ |
| 5 | **Global search (Cmd+K)** — command palette to search jobs, templates, inventories by name | High | ✅ |

### System Usage Bar (navbar)

| # | Enhancement | Priority | Status |
|---|-------------|----------|--------|
| 6 | **Click to navigate** — clicking the usage bar opens `/infrastructure/instances` | High | ✅ |
| 7 | **Alert threshold** — bar pulses/blinks when capacity ≥ 90% | Medium | ✅ |
| 8 | **Per-node inline breakdown** — when multiple execution nodes exist, show a small bar per node (not just in tooltip) | Low | ✅ |

### Performance

| # | Enhancement | Priority | Status |
|---|-------------|----------|--------|
| 9  | **Virtual scrolling for large lists** — shared `PageTable` now supports row windowing for large pages, and Jobs plus Hosts enable it so high `perPage` views render only visible rows plus spacer rows while preserving existing sorting, pagination, selection, and actions | High | ✅ |
| 10 | **WebSocket-driven dashboard refresh** — replace 30s polling with real-time AWX WebSocket event stream | Medium | ✅ |
| 11 | **Prefetch navigation data** — preload sidebar resource counts (hosts, inventories, etc.) on app load | Low | ✅ |

### Operations

| # | Enhancement | Priority | Status |
|---|-------------|----------|--------|
| 12 | **Bulk job cancel** — "Cancel all running jobs" one-click action on the Jobs list | High | ✅ |
| 13 | **Job output full-screen mode** — expanded full-viewport view for the job output console | Medium | ✅ |

### Security & Administration

| # | Enhancement | Priority | Status |
|---|-------------|----------|--------|
| 14 | **Session timeout warning** — notify the user 2 min before session expiry with an option to extend | High | ✅ |
| 15 | **Audit log viewer** — surface Activity Stream in a prominent, filterable UI rather than buried under Administration | Medium | ✅ |

### Developer Experience

| # | Enhancement | Priority | Status |
|---|-------------|----------|--------|
| 16 | **OpenAPI → TypeScript types** — auto-generate TS interfaces from `make genschema` output to replace manually maintained `awx/ui/src/frontend/awx/interfaces/` files | Low | ✅ |
| 17 | **Accessibility: table header names** — add `screenReaderText` / `aria-label` to all visually-empty `<Th>` elements (expand-row, select-row, drag-handle columns) to silence the PatternFly "Th: Table headers must have an accessible name" console warning on every table page | Medium | ✅ |
| 18 | **Login page logo centering** — CSS fix so the AWX logo is properly centered in the right panel of the login page instead of being flush against the form card | Low | ✅ |
| 19 | **Login page background image** — full-page background image configurable from Settings → User Interface (below logo upload); paste URL or upload file ≤ 2 MB; stored in browser localStorage; login form card left-aligned at desktop; logo displayed inside the login card | Medium | ✅ |
| 20 | **Navbar logo size control** — S / M / L size-preset buttons in Settings → User Interface (below the logo upload); selected size stored in localStorage and applied to the masthead brand on every page load | Low | ✅ |
| 21 | **Custom login background image in DB** — upload or paste an https:// URL in Settings → User Interface; stored in `CUSTOM_LOGIN_BACKGROUND` AWX setting (database), served from `/api/` root before auth, no more localStorage | Medium | ✅ |
| 22 | **Remove first-letter icon placeholders from sidebar** — nav items without an explicit icon now show only their label text in expanded mode; first-letter fallback kept only for collapsed (icon-only) mode | Low | ✅ |
| 23 | **Catalog deploy dynamic field interpolation** — deploy form now resolves `dynamic_field_templates` placeholders (for example `{mnemonic}{cores}`) using current form values before submitting `extra_vars` | High | ✅ |
| 24 | **AWX PWA install branding** — AWX favicon/PWA generation now uses the full AWX logo artwork and explicit manifest names (`name` / `short_name`) so browser install prompts show `AWX` instead of the shared npm package name | Medium | ✅ |
| 25 | **AWX dev proxy HTTPS default** — webpack dev environment now defaults AWX API proxy traffic to `https://localhost:8043`, matching the Docker Compose backend and avoiding nginx `400 The plain HTTP request was sent to HTTPS port` errors when running `npm run awx` directly | High | ✅ |
| 26 | **Overview performance card API wiring** — removed the nonexistent global `/api/v2/job_events/` collection call and now follows valid per-job `related.job_events` links, eliminating overview console 404s while keeping failed-host metrics | Medium | ✅ |

---

## Terraform & Catalog Platform

Full Terraform lifecycle support inside AWX — templates, execution, inventory population, credential injection, and a self-service Catalog for end-to-end provisioning/decommissioning.

**Guiding principles:**
- Terraform templates are first-class citizens alongside Job Templates; they share the same extra-vars system, credentials, RBAC, and workflow node model.
- Catalog items are thin wrappers around Workflow Job Templates; all provisioning/decommissioning logic lives in workflows so every step is auditable.
- The first provider target is **Proxmox**; the same credential-type pattern is then repeated for VMware, AWS, Azure, GCP, and OCI.

---

### Phase A — Terraform Template Engine (Backend)

| # | Task | Priority | Status |
|---|------|----------|--------|
| A1 | **`TerraformJobTemplate` model** — new `UnifiedJobTemplate` subclass in `awx/main/models/terraform.py`; fields: `scm_project` (FK → Project), `terraform_dir` (path within repo, default `.`), `extra_vars` (JSON/YAML same as JobTemplate), `verbosity` (0–4), `target_inventory` (FK → Inventory, nullable), `target_group` (str, nullable), `destroy_on_delete` (bool) | High | ✅ |
| A2 | **`TerraformJob` model** — `UnifiedJob` subclass; mirrors `TerraformJobTemplate` fields at launch time; adds `terraform_operation` choice field: `apply` / `destroy` / `plan` | High | ✅ |
| A3 | **Django migrations** — create initial migration for both new models; `swappable_dependency` on `UnifiedJob`/`UnifiedJobTemplate` already handled by base | High | ✅ |
| A4 | **Task runner** (`awx/main/tasks/terraform.py`) — `RunTerraformJob` task class; steps: checkout project via Receptor, write `terraform.tfvars.json` from extra_vars, inject credential env vars, run `terraform init`, run `terraform plan -out=plan.tfplan`, run `terraform apply plan.tfplan` (or `terraform destroy`); stream stdout/stderr to job event system | High | ✅ |
| A5 | **Inventory population from Terraform output** — after successful `apply`, run `terraform output -json`; for each key matching `host_ip_*` or a configurable output key pattern, create/update `Host` records in `target_inventory`; add to `target_group` if set; store `terraform_resource_id` in host variables for later `destroy` | High | ✅ |
| A6 | **Extra-vars / variable precedence** — reuse AWX's existing `process_extra_vars` pipeline; support survey specs (same `SurveySpec` model); Terraform receives merged vars as `terraform.tfvars.json` | Medium | ✅ |
| A7 | **Credential injection** — new `CredentialType` injector class `TerraformProviderInjector`; maps credential fields → env vars before `terraform` subprocess; each cloud provider credential type registers its own injector | High | ✅ |
| A8 | **DRF serializers + views** — `TerraformJobTemplateSerializer`, `TerraformJobSerializer`, `TerraformJobTemplateList/Detail`, `TerraformJobList/Detail`, `TerraformJobTemplateLaunch`, `TerraformJobCancel`; URL: `/api/v2/terraform_job_templates/`, `/api/v2/terraform_jobs/` | High | ✅ |
| A9 | **RBAC access classes** — `TerraformJobTemplateAccess`, `TerraformJobAccess` in `awx/main/access.py`; mirrors `JobTemplateAccess` permission model (admin/execute/read) | High | ✅ |
| A10 | **Workflow node type** — extend `WorkflowJobTemplateNode.unified_job_template` polymorphic FK to include `TerraformJobTemplate`; update workflow task manager to launch `TerraformJob` when a node resolves to a terraform template; update workflow visualiser API to return node type info | High | ✅ |
| A11 | **Execution Environment support for Terraform** — `TerraformJobTemplate` and `TerraformJob` inherit `execution_environment` FK from `UnifiedJobTemplate`/`UnifiedJob` base via `ExecutionEnvironmentMixin`; `RunTerraformJob._run_cmd()` detects a configured EE via `instance.resolve_execution_environment()` and wraps every `terraform` invocation in `{runtime} run --rm -v {private_data_dir}:/runner:Z --workdir {mapped_cwd} --env-file /runner/env/envvars {image} terraform {args}`; credential env vars are written to an env-file so they never appear in process listings; pull policy from `ExecutionEnvironment.pull` is honoured; **when no EE is explicitly configured the task runner automatically falls back to `hashicorp/terraform:latest`** (container runtime auto-detected: `docker` preferred, `podman` fallback) — the AWX execution node never needs Terraform installed locally | High | ✅ |

---

### Phase B — Cloud Provider Credential Types

Each credential type is a `CredentialType` fixture/data migration with `inputs` schema and `injectors` that map fields to env vars consumed by the relevant Terraform provider.

| # | Provider | Credential Fields | Env Vars Injected | Priority | Status |
|---|----------|-------------------|-------------------|----------|--------|
| B1 | **Proxmox VE** | `pm_api_url`, `pm_api_token_id`, `pm_api_token_secret`, `pm_node`, `pm_tls_insecure` (bool) | `PM_API_URL`, `PM_API_TOKEN_ID`, `PM_API_TOKEN_SECRET`, `PM_NODE`, `PM_TLS_INSECURE` | High | ✅ |
| B2 | **VMware vSphere** | `vsphere_server`, `vsphere_user`, `vsphere_password`, `vsphere_allow_unverified_ssl` (bool) | `VSPHERE_SERVER`, `VSPHERE_USER`, `VSPHERE_PASSWORD`, `VSPHERE_ALLOW_UNVERIFIED_SSL` | Medium | ✅ |
| B3 | **AWS** | `aws_access_key_id`, `aws_secret_access_key`, `aws_session_token` (optional), `aws_default_region` | `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_SESSION_TOKEN`, `AWS_DEFAULT_REGION` | Medium | ✅ |
| B4 | **Azure** | `arm_subscription_id`, `arm_client_id`, `arm_client_secret`, `arm_tenant_id` | `ARM_SUBSCRIPTION_ID`, `ARM_CLIENT_ID`, `ARM_CLIENT_SECRET`, `ARM_TENANT_ID` | Medium | ✅ |
| B5 | **GCP** | `gcp_project`, `google_credentials` (JSON key file content) | `GOOGLE_PROJECT`, `GOOGLE_CREDENTIALS` (written to temp file, path exported) | Low | ✅ |
| B6 | **Oracle Cloud (OCI)** | `tenancy_ocid`, `user_ocid`, `fingerprint`, `private_key` (PEM), `region` | Written to `~/.oci/config` before `terraform init` | Low | ✅ |
| B7 | **Proxmox privilege separation warning** | `pm_api_token_id` help text updated (migration 0221) to warn that tokens with "Privilege Separation" enabled need `User.Audit` + `Sys.Audit` explicitly granted; includes step-by-step fix for Proxmox UI | High | ✅ |

---

### Phase C — Terraform Template UI

| # | Task | Priority | Status |
|---|------|----------|--------|
| C1 | **Terraform Templates list page** — `awx/ui/src/frontend/awx/resources/terraform/TerraformTemplates.tsx`; table with columns: Name, Project, Directory, Last Run, Actions; same PatternFly table pattern as Job Templates | High | ✅ |
| C2 | **Terraform Template detail/edit form** — tabs: Details (name, description, project, dir, verbosity), Variables (extra_vars editor, survey), Credentials (cloud provider picker), Inventory Target (inventory + group picker, output key pattern), Notifications, Schedules | High | ✅ |
| C3 | **Launch dialog** — reuse existing `LaunchPrompt` pattern; shows survey + extra-vars override before launch | High | ✅ |
| C4 | **Terraform Job output page** — real-time log streaming; ANSI colour support; separate tabs: `plan` output and `apply` output; "Download log" button | High | ✅ |
| C5 | **Terraform Job history list** — per-template job runs list; columns: status, operation (apply/destroy/plan), started, duration, triggered by | Medium | ✅ |
| C6 | **Workflow editor node type** — add Terraform Template as a selectable node type in the workflow visualiser; distinct icon (Terraform logo or wrench) to distinguish from Ansible job nodes | High | ✅ |
| C7 | **Sidebar nav entry** — add "Terraform Templates" under the Resources section of the sidebar navigation; use a suitable icon | High | ✅ |
| C8 | **Terraform templates in global search** — Terraform Job Templates appear in the `AwxGlobalSearch` Cmd+K palette (type `terraform_template`); queries `/terraform_job_templates/?name__icontains=…`; shown with `LayerGroupIcon`; navigates to the template detail page on selection | High | ✅ |
| C9 | **Terraform details + targeting UX hardening** — Terraform Job details now display `artifacts` alongside Extra Variables; Terraform template form now dynamically loads inventory groups after selecting a target inventory and persists selected group cleanly; workflow handoff can override downstream `limit` from `host_ip*` artifacts via `terraform_override_limit` toggle in workflow/deployment extra vars; survey save normalizes numeric defaults to prevent integer/float default type errors | High | ✅ |

---

### Phase D — Catalog System (Backend)

| # | Task | Priority | Status |
|---|------|----------|--------|
| D1 | **`CatalogItem` model** (`awx/main/models/catalog.py`) — fields: `name`, `description`, `icon_url`, `provision_workflow` (FK → `WorkflowJobTemplate`, nullable), `terraform_job_template` (FK → `TerraformJobTemplate`, nullable), `deprovision_workflow` (FK → `WorkflowJobTemplate`, nullable), `extra_vars_schema` (JSON Schema for user-visible parameters), `organization` (FK → Organization) | High | ✅ |
| D2 | **`CatalogDeployment` model** — fields: `catalog_item` (FK), `name` (user-supplied label), `owner` (FK → User), `status` choices: `pending` / `provisioning` / `active` / `deprovisioning` / `failed` / `destroyed`; `provision_job` (FK → WorkflowJob, nullable), `terraform_provision_job` (FK → TerraformJob, nullable), `deprovision_job` (FK → WorkflowJob, nullable), `extra_vars` (JSON, the values the user filled in at deploy time), `created`, `modified`, `deployed_hosts` (M2M → Host) | High | ✅ |
| D3 | **`catalog_user` role** — built-in role; auto-assigned to any user account that has zero explicit role assignments at login time (post-login signal); grants: read-only view of CatalogItems they have visibility to, launch provision workflow on visible items, view own CatalogDeployments | High | ✅ |
| D4 | **`catalog_admin` role** — built-in role; grants: full CRUD on CatalogItems and all CatalogDeployments within scope; cannot access non-catalog AWX resources | High | ✅ |
| D5 | **Default role assignment signal** — `post_save` / login signal on `User`: if a new user has zero explicit role assignments in a single-organization install, assign the org-scoped `catalog_user`/member role; multi-org installs require explicit org membership or direct CatalogItem grants so default onboarding cannot cross tenant boundaries | High | ✅ |
| D6 | **DRF API** — `/api/v2/catalog_items/`, `/api/v2/catalog_deployments/`; actions: `POST /catalog_items/{id}/deploy/` (creates CatalogDeployment + launches provision workflow), `POST /catalog_deployments/{id}/deprovision/` (launches deprovision workflow + sets status) | High | ✅ |
| D7 | **RBAC access classes** — `CatalogItemAccess`, `CatalogDeploymentAccess`; catalog_admin sees all in org; catalog_user sees items they've been given read access to | High | ✅ |
| D8 | **Deployment status tracking** — WorkflowJob completion signal updates `CatalogDeployment.status`; populates `deployed_hosts` from the inventory populated by the Terraform job in the workflow | Medium | ✅ |
| D9 | **Catalog ↔ Terraform integration (backend)** — `CatalogItem.terraform_job_template` FK; `CatalogDeployment.terraform_provision_job` FK; `CatalogItemDeploy.post()` launches `TerraformJob` when terraform template is set (falls back to provision workflow otherwise); serializers expose both fields with summary and related links; migrations 0220 (FKs) + 0221 (Proxmox help text) | High | ✅ |

---

### Phase E — Catalog UI

| # | Task | Priority | Status |
|---|------|----------|--------|
| E1 | **Catalog browse page** (`/catalog`) — card grid of available `CatalogItem`s; each card shows icon, name, description, "Deploy" button; PatternFly `Gallery` layout | High | ✅ |
| E2 | **Deploy form dialog** — wizard: Step 1 extra-vars (JSON Schema-driven form), Step 2 deployment name + confirm; launches provision workflow and creates `CatalogDeployment` | High | ✅ |
| E3 | **My Deployments page** (`/catalog/deployments`) — table of own deployments; columns: name, catalog item, status, deployed hosts (count), deployed date; row action: "Deprovision" | High | ✅ |
| E4 | **Deployment detail page** — shows deployment vars, workflow job link (with status), list of provisioned hosts with IPs, decommission button | Medium | ✅ |
| E5 | **Catalog Admin: Item management** (`/catalog/admin/items`) — CRUD table; create/edit item links provision/deprovision workflows; visible only to catalog_admin + superuser | High | ✅ |
| E6 | **Catalog Admin: All Deployments** (`/catalog/admin/deployments`) — same as My Deployments but shows all users' deployments across the org; allows force-deprovision | High | ✅ |
| E7 | **Sidebar nav** — new top-level "Catalog" section in sidebar with entries: Browse, My Deployments; Admin sub-section (Items, All Deployments) shown only if user has catalog_admin | High | ✅ |
| E8 | **Catalog-only login redirect** — if logged-in user has only `catalog_user` role, redirect from AWX root (`/`) to `/catalog` instead of the usual dashboard | Medium | ✅ |
| E9 | **Catalog ↔ Terraform integration (UI)** — `CatalogItemForm` includes `terraform_job_template` selector alongside workflow selectors; catalog item list "Provision" column shows terraform template name with `(Terraform)` badge or workflow name; catalog item detail page shows terraform template row; deployment detail page links to the `TerraformJobPage` output viewer; `CatalogBrowse` cards show terraform template name indicator | High | ✅ |
| E10 | **Catalog dynamic field UX hardening** — removed dynamic-field controls from Name template; survey fields are shown automatically in Catalog Item create/edit for centralized per-field control (no manual add/remove); dynamic values are stored independently in `dynamic_field_templates`; admins can set both `deploy_disabled_fields` and `deploy_hidden_fields` per catalog item to control deploy-form behavior; item form now uses Details/Form Fields tabs with consistent two-column layout and icon URL input removed | High | ✅ |
| E11 | **Catalog sidebar VM size discoverability** — updated Catalog admin nav label to "Catalog Items / VM Sizes" so VM size preset management is visible from the sidebar | Medium | ✅ |
| E12 | **Dedicated VM Sizes admin home** — new Catalog sidebar item `VM Sizes` with hypervisor/public-cloud tabs, per-item VM size add/edit/remove controls, and provider metadata fields (`cluster`, `node`) for future placement mapping | High | ✅ |
| E13 | **Public cloud provider strategy tabs** — added provider tabs under VM Sizes planning view; DigitalOcean enabled as first test provider tab while preserving hypervisor-only custom size enforcement | High | ✅ |
| E14 | **Marketplace template ingestion plan** — design phase for importing cloud marketplace images/templates and provider-native plans into a provider-mapping layer consumable by Catalog deploy flows | High | ✅ |
| E15 | **Workflow-driven post-provision configuration mapping** — design phase for chaining template deploy + AWX workflows (configure, validate, lifecycle actions) across hypervisors and public clouds | High | ✅ |
| E16 | **Cloud section with connection health** — new top-level `Cloud` sidebar area with provider tabs on `Connections`, credential selection, connect/disconnect actions, and status indicators (green connected, warning/error states for misconfigured/disconnected) | High | ✅ |
| E17 | **Dynamic cloud provider sub-navigation** — when a provider is connected, a provider-specific child item appears under `Cloud` and opens dedicated configuration controls for template pull policy, allowed templates, and network allow-lists | High | ✅ |
| E18 | **Cloud RBAC hardening** — Cloud sidebar visibility and cloud management pages now require admin-level access (superuser/system auditor); credential `use` capability is enforced before provider connection attempts | High | ✅ |
| E19 | **DigitalOcean cloud credential type** — added managed migration for `DigitalOcean (Terraform)` credential type (`digitalocean_terraform`) so users can create and select DigitalOcean credentials in AWX | High | ✅ |
| E20 | **Automated DigitalOcean image + pricing pull** — Cloud provider settings now call a backend endpoint that uses the selected DigitalOcean credential to pull all available images and droplet pricing, then renders both lists in the UI without manual source URL input | High | ✅ |
| E21 | **DigitalOcean Overview tab** — first tab on the DigitalOcean provider page shows a connection card with credential info, four stat boxes (images, droplet sizes, regions, networks) pulling from pulled data, and an info panel with provider description and last-pull timestamp | High | ✅ |
| E22 | **Azure VM Sizes pricing column** — VM Sizes tab in Azure provider now fetches live Linux on-demand prices from the Azure Retail Prices API (public, no auth) per primary region and displays a `Price/hr` column alongside CPU/memory/GPU specs | High | ✅ |
| E23 | **Provider page header layout** — action buttons (`Pull data`, `Manage connections`) moved to page header right side; `Last pull: [timestamp]` shown right-aligned below the buttons; applies to both DigitalOcean and Azure provider pages | Medium | ✅ |
| E24 | **Catalog deployment page header** — deployed date and action buttons (`Retry provision`, `Deprovision`) with inline status badge moved from the Details tab into the page header right side; Details tab now shows only data fields | Medium | ✅ |
| E25 | **Catalog browse provider logos** — show actual, borderless cloud provider logos (DigitalOcean, Azure, AWS, VMware, GCP, Proxmox) on catalog cards instead of circular abbreviations | Medium | ✅ |
| E26 | **Inventory/group targeting moved to provider Fields tabs** — Target inventory (dropdown) and Target group (text input) moved from the Cloud providers tab into each provider's dedicated Fields tab in the Catalog Item edit form; values are loaded from the linked TFT node and immediately PATCHed on change | Medium | ✅ |
| E27 | **Provisioning history workflow topology view** — expanded row in the provisioning history table now renders a full PatternFly `@patternfly/react-topology` Dagre graph matching the AWX workflow visualizer: same node styles/icons/status colors, "Run on success / failure / always" edge labels, zoom-in/out/fit control bar, and clickable nodes that navigate to the job output page | High | ✅ |
| E28 | **Azure + DigitalOcean workflow surveys and deprovision workflows** — added reproducible `seed_cloud_provider_workflows` command that creates/updates Azure + DigitalOcean Terraform project wiring, inventories/groups, provision/deprovision TerraformJobTemplates, survey-enabled WorkflowJobTemplates, and a multi-cloud CatalogItem wired through `provider_workflows` / `provider_deprovision_workflows`; surveys cover Azure provision (`vm_name`, `resource_group_name`, `vnet_name`, `subnet_name`, `vm_size`, `admin_username`), Azure deprovision (`vm_name`, `resource_group_name`, `vnet_name`, `subnet_name`), DigitalOcean provision (`droplet_name`, `do_region`, `do_droplet_size`, `do_image`, `do_ssh_key_name`), and DigitalOcean deprovision (`droplet_name`) | High | ✅ |
| E29 | **Cloud provider table UX improvements** — verbose/wide columns moved to expandable rows across all three provider pages (Azure: URN/ID on VMImages, GPU/MaxDisks/MaxNICs/Premium SSD/Accel.Net/Zones/Ultra SSD on VMSizes, SKU on Storage, Size on VMs; Proxmox: Type/Uptime on Templates; VMware: ID on VMs/Hosts/Networks/Datacenters, DatastoreID on Datastores); per-row Switch toggles replaced with compact `Allowed`/`Denied` Label badges; toolbar bulk-action buttons ("Allow selected" / "Deny selected") added to catalog-control tabs (Azure: Locations/VMImages/VMSizes; Proxmox: Templates; VMware: Datastores/Networks) using `PageActionSelection.Multiple` to enable row checkboxes | High | ✅ |
| E30 | **Deployment TTL / Auto-expiry** — `CatalogItem.default_lease_minutes` sets a default lease on every new deployment; `CatalogItem.require_lease` forces deployers to supply a TTL; `CatalogDeployment.expires_at` + `auto_deprovision` track the lease; a periodic system task scans for expired active deployments and fires the deprovision workflow; deploy form Step 2 gains an optional TTL picker (preset buttons: 2 h / 8 h / 24 h / 7 d + custom date); My Deployments table shows a colour-coded countdown badge (green > 4 h, yellow ≤ 4 h, red ≤ 1 h) | High | ✅ |


---

### Phase F — Proxmox End-to-End Reference Implementation

Complete working example: Proxmox VM → Ansible configuration → Catalog item.

| # | Task | Priority | Status |
|---|------|----------|--------|
| F1 | **Proxmox credential type** — implement B1 as a loaddata fixture (`awx/main/fixtures/credential_type_proxmox.json`) so it ships with the dev environment | High | ✅ |
| F2 | **Terraform template project** — sample Git repo (or `awx_devel` sub-path) with: `main.tf` using `telmate/proxmox` provider; variables: `vm_name`, `cores`, `memory`, `disk_gb`, `proxmox_template_name`, `ip_address`, `gateway`; outputs: `host_ip` (maps to AWX inventory population) | High | ✅ |
| F3 | **AWX Terraform template** — pre-seeded dev fixture: project pointing at F2 repo, Proxmox credential attached, `target_inventory` set, `target_group = "proxmox_vms"` | High | ✅ |
| F4 | **Ansible playbook template** — Job Template using a playbook that: installs Apache / Nginx on the newly added host; limits to `proxmox_vms` group; uses the host IP populated by F3 | High | ✅ |
| F5 | **Workflow** — two-node workflow: Node 1 = Terraform template (F3), Node 2 (on success) = Ansible template (F4); demonstrates passing inventory context between nodes | High | ✅ |
| F6 | **Catalog item** — `CatalogItem` fixture: "Apache on Proxmox VM"; provision_workflow = F5; deprovision_workflow = separate workflow that runs `terraform destroy` via a TerraformJob with `terraform_operation=destroy` | High | ✅ |
| F7 | **Documentation** — `docs/terraform_catalog.md`: architecture overview, credential setup, template configuration, inventory output mapping convention, Proxmox walkthrough | Medium | ✅ |

---

### Implementation Order (Suggested Sprint Sequence)

```
Sprint 1  →  A1–A3 (models + migrations) + B1 (Proxmox credential type)
Sprint 2  →  A4–A5 (task runner + inventory population)
Sprint 3  →  A6–A9 (extra-vars, injectors, API, RBAC)
Sprint 4  →  C1–C4 (Terraform template list, form, launch, job output UI)
Sprint 5  →  A10 + C6 (workflow node support, visualiser)
Sprint 6  →  D1–D5 (Catalog models + roles)
Sprint 7  →  D6–D8 (Catalog API + status tracking)
Sprint 8  →  E1–E4 (Catalog browse + deploy + deployments UI)
Sprint 9  →  E5–E8 (Catalog admin UI + role-based redirect)
Sprint 10 →  F1–F7 (Proxmox end-to-end + docs)
Sprint 11 →  B2–B6 (remaining cloud provider credential types)
```

---

---

## Phase G — AI & Platform Intelligence *(Future Development — AAP Parity)*

Features present in Red Hat Ansible Automation Platform (AAP) that are not yet in this AWX fork. These require significant backend and UI work and are tracked here for future planning.

### G1 — Ansible Lightspeed AI Assistant

| # | Task | Priority | Status |
|---|------|----------|--------|
| G1a | **Embedded chat assistant UI** — generative AI chat panel accessible from the masthead or a dedicated route; answers platform admin and management queries in natural language; assistant panel width increased for easier resource-plan review on desktop while preserving mobile viewport constraints | High | ✅ |
| G1b | **Playbook / task coding assistant** — AI-generated extra-vars/Terraform variable suggestions are inline; AI resource actions can author survey specs/questions for job, workflow, and Terraform templates; and same-plan `project` + `project_file` + `job_template` refs can create a new manual Project workspace, write playbook/role files, then create Job Templates that point at those files with rollback on failure | High | ✅ |
| G1c | **BYOM (Bring Your Own Model) settings** — Settings page section to configure the AI provider: Red Hat AI, OpenAI, Azure OpenAI, IBM watsonx, or Google Gemini; stores endpoint URL + API key in AWX settings (encrypted) | Medium | ✅ |
| G1d | **Backend AI proxy** — Django view that forwards chat/completion requests to the configured model provider; masks credentials from the browser; enforces per-user rate limits | High | ✅ |
| G1e | **OpenAI Codex device-login provider** — AI Assistant settings now include OpenAI Codex device-code authentication as a separate provider alongside existing OpenAI API-key support; tokens are stored encrypted and refreshed for ChatGPT Codex responses backend calls | High | ✅ |
| G1f | **OpenAI Codex model catalog/default selection** — Codex device-login settings can refresh available models, fall back to a curated Codex catalog when `/v1/models` is unavailable for OAuth tokens, and save the selected default model used by AI chat | High | ✅ |

### G2 — Automation Insights Dashboard

| # | Task | Priority | Status |
|---|------|----------|--------|
| G2a | **ROI / value measurement dashboards** — compute and display automation ROI (hours saved, cost avoidance) based on job runtimes and configurable host/hour values | Medium | ✅ |
| G2b | **Real-time actionable insights** — overview insights now refresh every 30 seconds from RBAC-filtered job, project, inventory, and inventory-source endpoints; the card detects failure-rate spikes, slow-runtime anomalies, failed/stale inventory sources, active host failures, and renders deterministic recommendations with optional manual AI summarization | Medium | ✅ |
| G2c | **Performance metrics panels** — expanded dashboard card now supports configurable 24h / 7d / 30d windows, current execution-node capacity, execution-load trend buckets from recent jobs, slowest successful templates, and most-failed hosts from recent job events | Medium | ✅ |

### G3 — MCP Server (Model Context Protocol)

| # | Task | Priority | Status |
|---|------|----------|--------|
| G3a | **MCP server endpoint** — expose AWX resources (job templates, inventories, credentials, deployments) via the Model Context Protocol so external AI agents can discover and invoke automation without custom integrations | High | ✅ |
| G3b | **RAG policy injection** — MCP policy/best-practice context is embedded into a deterministic local hash-vector index, ranked by vector similarity plus matched terms, returned to MCP agents, and injected into OPA launch guardrail input | Medium | ✅ |
| G3c | **MCP auth + audit** — MCP tools publish `mcp:read` / `mcp:write` OAuth2 scope requirements with AWX `read` / `write` compatibility, scoped OAuth tokens are enforced before tool invocation, and every MCP-initiated action is logged to the Activity Stream with `triggered_by: mcp_agent` | High | ✅ |

### G4 — AI-Assisted Inventory Generation

| # | Task | Priority | Status |
|---|------|----------|--------|
| G4a | **Natural-language inventory builder** — UI wizard where users describe their infrastructure in plain text; AI generates a structured AWX inventory (groups, hosts, variables) as a preview before saving | Medium | ✅ |
| G4b | **Cloud resource → inventory AI mapping** — extend the existing cloud provider pull to use an AI step that suggests inventory group structure and variable mappings from pulled resource metadata | Medium | ✅ |

### G5 — Automation Orchestrator (Multi-Mode Canvas)

| # | Task | Priority | Status |
|---|------|----------|--------|
| G5a | **Multi-mode workflow canvas** — extend the workflow visualiser to support three node types side-by-side: deterministic (existing Ansible/Terraform/approval/sync), event-driven (EDA rulebook activations), and AI-driven (AI task nodes); the canvas now persists/edits all three node modes, shows explicit All/Deterministic/EDA/AI mode counts and focus controls without treating focused-out nodes as deleted, includes Terraform/EDA/AI legend entries, and preserves job/skip tag prompt overrides correctly on save | High | ✅ |
| G5b | **Event-Driven Ansible (EDA) integration** — connect AWX to an EDA Controller instance; `/api/v2/eda/status/`, `/api/v2/eda/activations/`, `/api/v2/eda/activations/start/`, activation detail, events, enable/disable/restart, and delete endpoints now provide shared backend status/activation context plus admin-gated activation create/start/control behavior, with encrypted token/SSL/timeout/path settings and live activation reads when configured; workflow EDA metadata nodes can be created/edited/saved in the visualizer, create/start/poll controller activations at runtime, expose EDA metadata/actions/events/status to downstream nodes, route failed activation checks through workflow failure paths, and the AWX UI now includes an Event-Driven section with native activation list/detail/event log CRUD controls | High | ✅ |
| G5c | **AI-driven node type** — persisted `ai_task` workflow nodes now delegate execution-plan generation to the configured AI provider at runtime, store provider/model/response/plan/error artifacts for downstream nodes, route provider failures through workflow failure paths, validate AWX resource-action plans through the shared AI preview engine, create pending `WorkflowApproval` records for human approval by default, and apply/deny approved plans from the central Workflow Approvals notification section with RBAC, audit, rollback, and graph resume behavior | Medium | ✅ |

### G6 — OPA (Open Policy Agent) Guardrails

| # | Task | Priority | Status |
|---|------|----------|--------|
| G6a | **OPA policy engine integration** — backend middleware evaluates OPA policies before job, workflow, Terraform, system, catalog, MCP, and AI resource launches; policies are expressed in Rego, stored in `OPA_POLICY_BUNDLE`, synced to the real OPA Policy API from the Policy as Code sidebar, and evaluated through OPA's Data API | High | ✅ |
| G6b | **Policy status and tester UI** — Policy as Code has its own sidebar section with Overview, Gatekeeper, Policy Modules, Policy Tester, and Smoke Test pages; Overview shows OPA enforcement status, server URL, managed bundle size, and registered decision paths; Policy Tester posts sample JSON to the real OPA Data API adapter; Settings → Policy Connections keeps OPA/Gatekeeper server/auth/timeout configuration only | Medium | ✅ |
| G6c | **AI-action guardrails** — OPA policy checks now cover API, MCP, and workflow AI resource actions with source, mode, destructive-operation classification, privileged-resource classification, human-approval flags, and workflow approval metadata so real OPA policies can block destructive AI actions unless a human approval path was used | High | ✅ |
| G6d | **OPA policy module manager** — Policy as Code → Policy Modules now manages live plain-OPA modules directly through the OPA Policy API: list policy IDs/packages/rules, view decision paths, create/update/delete Rego modules with OPA validation, compile-error display, Activity Stream-backed raw Rego version history, audited rollback to before/after snapshots, OPA-only smoke evaluation, and sync status for AWX-managed policy IDs | High | ✅ |
| G6e | **Gatekeeper Policy Manager parity view** — Policy as Code → Gatekeeper now has a GPM-inspired view backed by the configured Kubernetes API: explicit cluster/config/auth error states, named Kubernetes context selection through the Gatekeeper context map, ConstraintTemplates with target/Rego/lib/schema/status metadata and route-backed drill-down permalinks, Constraints sorted by violations with enforcement/match/parameters/status route-backed drill-down, Config CRD sync/readiness route-backed drill-down, query-backed violation search/sort/page/per-page controls, true paginated violation tables, and downloadable JSON report; governed write actions are tracked in G6f | High | ✅ |
| G6f | **OPA/Gatekeeper write governance** — Policy as Code → Gatekeeper now has governed manifest apply, delete, and rollback paths for ConstraintTemplates, Constraints, and Config CRDs: preview shows target/diff/rollback without Kubernetes writes, dry-run/apply/delete/rollback use the configured Kubernetes API, write paths are checked through the `awx/gatekeeper_resource/allow` OPA decision path, write attempts create Activity Stream audit entries with target/checksum/rollback/apply-strategy metadata, apply/restore can use Kubernetes server-side apply with field-manager and force-conflicts controls, and AI-assisted manifest authoring uses visible AWX/Gatekeeper context before filling the governed manifest workflow | High | ✅ |

---

## Phase H — Platform Hardening

### H1 — Multi-tenancy / Org Isolation

| # | Task | Priority | Status |
|---|------|----------|--------|
| H1 | **Hard namespace boundaries between orgs in Catalog and Cloud** — `CloudProviderConnection` and `CloudProviderState` gain `organization` FKs (migration 0245); cloud connection/state RBAC scopes reads and writes to system users or org admins; provider-state, validate, and pull endpoints resolve accessible org-scoped connections before touching provider data; Catalog deploy/VM-size/provider views pass organization context so cloud resources, pulled provider data, and catalog controls stay isolated per org | High | ✅ |
| H2 | **Org isolation endpoint parity matrix** — added functional coverage for org-admin Catalog item/deployment list scope, cross-org Terraform/workflow launch rejection, deprovision/retry permission boundaries, and every Cloud validate/pull endpoint rejecting foreign or mismatched connection organization context; frontend Catalog Item cloud-provider wiring now filters connections by the selected item organization | High | ✅ |
| H3 | **Catalog job-link route parity** — Catalog deployment details, My Deployments, All Deployments, and provisioning history now route workflow jobs through `/jobs/workflow/:id/output` and Terraform jobs through `/terraform-templates/jobs/:job_id/output`; Terraform history rows no longer attempt to render workflow topology and route params use `job_id` consistently | High | ✅ |
| H4 | **Marketplace ingestion org-isolation parity** — Marketplace imports now require an administered organization, never create unscoped global CatalogItems by omission, reject foreign-org workflow links during import, and the Marketplace modal posts the selected accessible organization so imported Catalog Items are properly wired into the same org boundary as the rest of Catalog | High | ✅ |
| H5 | **Catalog admin access + cancel endpoint parity** — Catalog admin sidebar entries and direct admin routes now require superuser or organization-admin access, with non-admin users receiving an unauthorized state instead of dead admin links; Catalog deployment cancel now has an explicit `cancel` access check so owners can cancel their own deployments, org admins can cancel same-org deployments, and forged or foreign-org cancel requests are rejected | High | ✅ |
| H6 | **CatalogItem direct RBAC + audit parity** — CatalogItems now register with DAB RBAC and Activity Stream, include an object-level `use_catalogitem` permission, and direct per-item `use_role` grants unlock Catalog deploy endpoints while syncing RoleUserAssignment and audit records | High | ✅ |
| H7 | **Default catalog user org-isolation hardening** — The default catalog-user signal no longer adds new users to every organization; automatic member-role assignment is limited to unambiguous single-org installs, while multi-org environments require explicit org or direct CatalogItem grants | High | ✅ |
| H8 | **EDA settings endpoint wiring parity** — The done-marked EDA overview card now points at a registered `/api/v2/settings/eda/` category backed by `EDA_SERVER_URL`, and the Settings navigation includes Event-Driven Ansible so the overview card no longer calls a missing endpoint | High | ✅ |
| H9 | **OPA settings/API parity hardening** — OPA overview status and the policy tester now use the registered Policy as Code settings (`OPA_HOST`, `OPA_PORT`, `OPA_SSL`, auth, timeout/retry settings) instead of stale `OPA_ENABLED`/`OPA_SERVER_URL` fields; structured OPA responses with `allowed: false` now correctly deny; policy status/evaluation is restricted to system admins; token auth headers are copied per request so settings are not mutated | High | ✅ |
| H10 | **Catalog lifecycle chaining parity** — Catalog configure/validate workflow chaining now persists `configure_job` and `validate_job`, passes saved deployment variables/artifacts into lifecycle workflows, advances idempotently through provisioning → configuring → validating → active, and runs after both Workflow and Terraform-backed catalog provisioning jobs | High | ✅ |
| H11 | **Marketplace provider-state source parity** — Marketplace template listing and import now prefer org-scoped `CloudProviderState` data from connected providers, respect provider admin allow-lists, reject foreign organization reads/imports, and only fall back to the static seed catalog when no pulled provider state exists for the selected organization | High | ✅ |
| H12 | **Marketplace deployment-target import parity** — Marketplace imports can now wire either an org-scoped Terraform template or provision workflow plus optional deprovision/configure/validate workflows; the modal requires a launch target before import, posts the selected lifecycle IDs, and imported provider cards are backed by `cloud_backends` / `provider_workflows` so Catalog browse deployments are immediately launchable | High | ✅ |
| H13 | **Catalog lease API enforcement parity** — Catalog deploy now persists `expires_at` / `auto_deprovision`, validates ISO-8601 future expiry values, rejects `auto_deprovision` without a lease, and enforces `CatalogItem.require_lease` at the API boundary so TTL settings cannot be bypassed outside the UI | High | ✅ |
| H14 | **Catalog lease expiration lifecycle parity** — Expired active deployments now transition to an explicit `expired` lifecycle state when auto-deprovision is off or no valid same-org deprovision workflow is available; auto-expiry uses the same saved variables/artifacts as manual deprovision and refuses cross-organization deprovision workflow mappings | High | ✅ |
| H15 | **MCP resource, policy context, and audit parity** — MCP tools now expose Catalog Items and Catalog Deployments through the same RBAC scope as the REST API, return configured policy/best-practice context with tool responses and OPA launch guardrail input, and write Activity Stream records with `triggered_by: mcp_agent` for every invoked tool | High | ✅ |
| H16 | **AI inventory builder save parity** — Natural-language inventory output is parsed into an AWX inventory plan, previewed as groups/hosts/variables, and applied through the existing inventory, group, and host endpoints after save instead of being written into the inventory variables field; generated resources remain scoped to the selected inventory organization and RBAC boundary | High | ✅ |
| H17 | **Cloud provider inventory mapping parity** — Provider-state data from DigitalOcean, Proxmox, VMware, and Azure now exposes an org-scoped `/inventory_suggestions/` API action that maps pulled resource metadata into AWX inventory groups/hosts/variables, optionally refines through the configured AI provider, falls back deterministically when AI is unavailable, and wires every cloud provider page to preview and create an inventory from the generated plan | High | ✅ |

---

## Phase I — AI-Native Resource Authoring & Cross-App Wiring

The AI assistant should become a reviewable AWX resource authoring layer, not isolated helper buttons. These items track the next implementation slice.

| # | Task | Priority | Status |
|---|------|----------|--------|
| I1 | **Unified AI action framework** — backend endpoint that turns natural-language requests into typed AWX resource plans, validates them against serializers/RBAC/OPA, returns a diff/preview, and only applies changes after explicit user approval; `/api/v2/ai/resource_actions/` now supports prompt-generated or supplied plans, preview/apply modes, explicit Activity Stream audit, OPA guardrail checks, and serializer/RBAC validation | High | ✅ |
| I2 | **AI resource creation coverage** — support create/update plans for projects, project files/playbooks/roles, job templates, workflow job templates, inventories, inventory groups, hosts, smart inventories, constructed inventories, inventory sources, schedules, credential references, catalog items, role assignments, and survey specs/questions without bypassing existing AWX permissions; backend coverage now includes same-plan refs for project, inventory, group, and host authoring plus serializer/RBAC/OPA validation for all listed resource types | High | ✅ |
| I3 | **AI playbook and role authoring** — generate playbooks, task files, handlers, defaults, vars, meta, and README content into a project-backed content workspace, then create/update AWX Project and Job Template records that point at the generated files; `/api/v2/ai/resource_actions/` now supports safe `project_file` create/update operations, new manual Project workspace creation via `create_local_path`, same-plan `project_ref` wiring into file and Job Template ops, path traversal/hidden SCM path guards, rollback of files/workspaces on later validation failure, and Activity Stream Project/Job Template audit links | High | ✅ |
| I4 | **AI smart/constructed inventory authoring** — generate and validate smart inventory `host_filter` expressions and constructed inventory `source_vars`, including a preview of matching hosts/groups before save; `/api/v2/ai/resource_actions/` now previews smart host/group matches, validates constructed `source_vars`, previews constructed input inventories/source hosts/groups, saves constructed input inventory links on apply, and the global AI assistant renders the preview metadata before approval | High | ✅ |
| I5 | **Contextual AI actions throughout the app** — add consistent AI entry points to resource create/edit/detail pages and pass current route/resource context so suggestions are grounded in the object being edited; the global masthead AI assistant now exposes a resource-plan preview/apply flow, and create/edit/detail routes show a contextual AI launcher that preloads page-kind/resource context and sends path/search/hash/title context to `/api/v2/ai/resource_actions/` | High | ✅ |
| I6 | **Workflow plan apply** — upgrade the workflow AI suggestion panel from plain text guidance to a structured plan that can add deterministic, approval, Terraform/existing AWX executable nodes, EDA metadata nodes, and persisted AI task nodes to the visualizer with preview before save; the panel now requests strict JSON, resolves proposed nodes against visible Unified Job Templates and EDA context, adds approved unsaved nodes/edges into the visualizer for normal Save persistence, and runtime AI task resource-action plans pause for human approval before applying | High | ✅ |
| I7 | **EDA node parity** — represent EDA controller connections, rulebook activations, and event source status as first-class workflow visualizer node choices; backend EDA status/activation facade, encrypted controller auth settings, admin-gated activation create/start/control endpoints, workflow AI planning context, persisted workflow node metadata, visualizer create/edit/save paths, workflow launch preflight, runtime controller create/start/poll status sync, success/failure path handling, downstream artifact propagation, and AWX-native activation list/detail/event-log CRUD UI are wired | High | ✅ |
| I8 | **AI safety, audit, and rollback** — every AI-applied change writes Activity Stream audit metadata with provider/model/prompt summary, links created resources, returns an auditable rollback plan, supports RBAC/OPA-gated delete cleanup for generated drafts, and exposes rollback details in the global AI assistant | High | ✅ |

---

## Current Parity Audit Findings

Code review against the completed tracker found these remaining implementation gaps.

| # | Finding | Classification | Priority | Status |
|---|---------|----------------|----------|--------|
| AP1 | **Catalog post-provision lifecycle chain is not fully wired** — `configure_workflow` / `validate_workflow` fields exist and the UI exposes them, but signal persistence dropped `configure_job` / `validate_job`; Terraform-backed catalog deployments did not launch configure/validate workflows after a successful Terraform provision job | Implementation Bug | High | ✅ |
| AP2 | **Marketplace template source is static** — `/api/v2/marketplace/templates/` returns hard-coded provider catalogs instead of provider data already pulled into `CloudProviderState`; imports therefore do not reflect connected organization-scoped cloud inventory | Stub / Approximation | High | ✅ |
| AP3 | **Marketplace import is not deployment-ready from the modal** — the backend can accept optional workflow ids, but the UI only posts provider/template/name/organization, so imported items are browseable records that still need manual workflow/Terraform mapping before they can launch real provider deployments | Stub / Approximation | High | ✅ |
| AP4 | **Catalog deployment lease fields are UI-only** — E30 added TTL controls, model fields, serializers, and an expiry task, but `CatalogItemDeploy` did not persist `expires_at` / `auto_deprovision` and did not enforce `require_lease`, so API launches silently ignored TTL choices and could bypass required leases | Implementation Bug | High | ✅ |
| AP5 | **Catalog lease expiry never marks manual leases expired** — E30 promised deployments would be marked expired when a TTL elapsed, but `expire_catalog_deployments` only queried `auto_deprovision=True` rows; manual-expiry deployments stayed `active` forever, and the auto path did not reuse manual deprovision saved vars or reject stale cross-org workflow mappings | Implementation Bug | High | ✅ |
| AP6 | **MCP completion was partial** — G3 claimed catalog/deployment resource exposure, RAG policy injection, and Activity Stream audit, but the MCP tool catalog omitted Catalog Items/Deployments, policy context was not surfaced to agents or OPA guardrails, and `_tag_activity` only wrote a logger line instead of an auditable Activity Stream record | Stub / Approximation | High | ✅ |
| AP7 | **AI inventory builder did not save inventory structure** — G4a claimed natural-language output created a structured AWX inventory preview before saving, but the UI only inserted generated INI text into the `variables` YAML editor; no groups, hosts, child groups, or generated variable records were applied through AWX inventory endpoints | Stub / Approximation | High | ✅ |
| AP8 | **Cloud resource inventory mapping was missing** — G4b claimed pulled cloud resources were run through an AI mapping step to suggest AWX inventory structure, but provider state only stored raw resource payloads and the cloud pages had no inventory suggestion or create-inventory path | Stub / Approximation | High | ✅ |
| AP9 | **Automation Insights retried AI chat indefinitely** — G2b claimed dashboard AI recommendations were complete, but provider failures or rate-limit responses left the summary empty and caused the card to re-post to `/api/v2/ai/chat/` on repeated renders until the backend returned 429/502 storms | Implementation Bug | High | ✅ |
| AP10 | **Overview console noise from automatic AI insights and chart tooltip** — Overview no longer auto-posts AI insight generation on page load, avoiding background `429`/`502` console noise when the provider is unavailable or rate-limited; the job activity chart disables the PatternFly cursor tooltip path that emits the React `defaultProps` warning in development | Implementation Bug | High | ✅ |
| AP11 | **Catalog provider survey choices crashed Proxmox deploy** — Provider workflow surveys can return `choices` as arrays, but the catalog deploy wizard only accepted newline-delimited strings; Proxmox Apache deploy now normalizes both formats and preserves multiselect values as arrays for launch extra vars | Implementation Bug | High | ✅ |
| AP12 | **AI Assistant lacked live AWX grounding and markdown rendering** — The chat assistant could answer AWX instance questions with generic external guidance and rendered markdown as raw `**` / `-` text; it now answers direct visible resource count/list questions from RBAC-filtered AWX data, includes a bounded redacted AWX resource snapshot in the default prompt context, and renders assistant responses as markdown | Implementation Bug | High | ✅ |
| AP13 | **G1b was over-marked done** — `AICodeAssistant` is wired into Job Template extra variables and Terraform variables; `/api/v2/ai/resource_actions/` authors survey questions/specs for job, workflow, and Terraform templates; and AI can now create new manual Project workspaces, write playbook/role files, reference the generated Project from Job Templates in the same plan, and roll back generated workspace/files if later validation fails | Documentation Drift | High | ✅ |
| AP14 | **G5 workflow parity was over-marked done** — the workflow visualizer now has a human-approved AI plan preview/apply path for deterministic AWX, Terraform, approval, EDA metadata, and persisted AI task nodes, EDA controller/activation context is available to the planner, EDA metadata nodes create/start/poll configured controller activations at workflow runtime, AI task nodes generate runtime plan artifacts plus approval-gated resource-action execution, and native EDA activation CRUD/log UI is now available in the AWX Event-Driven section | Documentation Drift | High | ✅ |
| AP15 | **AI resource creation is not end-to-end** — typed backend plan/preview/apply now exists for projects, project files/playbooks/roles, job templates, workflow job templates, inventories, groups, hosts, smart inventories, constructed inventories, inventory sources, schedules, credential references for job templates, inventory sources, schedule launch prompts, and workflow-node launch prompts, user/team role assignments on object roles, catalog items, and survey specs/questions through `/api/v2/ai/resource_actions/`; same-plan refs wire project → files/templates and inventory → groups/hosts, the masthead assistant plus create/edit/detail contextual launcher expose frontend preview/apply approval paths, and workflow visualizer apply is covered separately in I6/AP14 | Stub / Approximation | High | ✅ |
| AP16 | **AI inventory coverage is narrow** — natural-language inventory save parity now covers smart inventory `host_filter` generation/validation plus constructed inventory `source_vars` validation, input inventory linking, preview, and save coverage through the shared AI resource action endpoint | Stub / Approximation | High | ✅ |
| AP17 | **AI features are not consistently contextual across the app** — the masthead assistant is globally available, passes normalized route context into AI resource planning, and every create/edit/detail route now exposes a contextual launcher that preloads page-kind/resource context before preview/apply | Documentation Drift | High | ✅ |
| AP18 | **MCP authoring parity** — MCP now exposes `preview_resource_action` and `apply_resource_action` tools backed by the shared AI resource-action engine, so agents can validate and apply typed plans for projects, project files/playbooks/roles, inventories, smart/constructed inventories, inventory sources, job templates, workflow templates, schedules, catalog items, role assignments, credential references, and survey specs through existing serializers, RBAC, OPA guardrails, audit, and rollback behavior; apply requires an explicit plan and `mcp:write` scope | Stub / Approximation | High | ✅ |
| AP19 | **Theme persistence is now server-side** — `UserUISettings.ui_preferences` stores allowed UI preferences per AWX user; `/api/v2/me/` exposes and patches the current user's preferences; `PageSettingsProvider` loads server values and saves edits back through the AWX active-user bridge while retaining localStorage fallback for anonymous/offline states | Documentation Drift | Medium | ✅ |
| AP20 | **VirtualizedTable was not implemented** — the installed PatternFly table package does not expose a ready `VirtualizedTable`, so `PageTable` now provides framework-level row windowing for large table pages; Jobs and Hosts opt in, keeping pagination/filter/sort/actions intact while avoiding thousands of rendered DOM rows | Stub / Approximation | High | ✅ |
| AP21 | **Navigation count prefetch was partial** — app startup now seeds SWR for overview data plus the core navigation/resource-count endpoints (`hosts`, `inventories`, `templates`, `projects`, `schedules`, Catalog, access, infrastructure, and admin-only cloud/settings endpoints when applicable) using small `page_size=1` requests | Documentation Drift | Low | ✅ |
| AP22 | **Azure/DigitalOcean workflow survey IDs are reproducible** — `awx-manage seed_cloud_provider_workflows` now creates/updates the Azure and DigitalOcean provision/deprovision TerraformJobTemplates, survey-enabled WorkflowJobTemplates, copied Terraform example project content, provider inventories/groups, and CatalogItem provider/deprovision maps; functional tests verify wiring and idempotency | Environment / Setup Issue | High | ✅ |
| AP23 | **Automation insights were job-statistics only** — the overview insights card now combines recent unified jobs, projects, inventories, and inventory sources with a 30-second refresh loop; it detects failure spikes, execution-time anomalies, inventory-source drift, active host failures, and produces local actionable recommendations while keeping AI summarization manual to avoid provider retry storms | Stub / Approximation | High | ✅ |
| AP24 | **Performance metrics lacked trend and failed-host analysis** — the overview performance card now calculates selected-window execution-load trend buckets from recent unified jobs and most-failed hosts from recent job events, while retaining current execution-node capacity and slowest-template metrics | Stub / Approximation | Medium | ✅ |
| AP25 | **MCP policy context is vector-ranked and scope-aware** — MCP policy context now builds a deterministic local hash-vector index from `MCP_POLICY_CONTEXT`, ranks relevant policy sections by vector similarity plus matched terms, hides internal embeddings from responses, exposes `mcp:read` / `mcp:write` scope metadata, and rejects scoped OAuth tool calls that lack the required read/write MCP scope while preserving AWX `read` / `write` token compatibility | Stub / Approximation | High | ✅ |
| AP26 | **OPA guardrail scope now covers human approval/destructive-action policy paths** — shared OPA launch enforcement runs before Job Template, Workflow Job Template, Terraform Job Template, System Job Template, and Catalog deployment launches; AI resource-action OPA input now includes API/MCP/workflow source, preview/apply mode, destructive/privileged flags, human-approved state, approval-required state, and workflow approval metadata. AWX remains an OPA adapter and sends decisions to real OPA instead of embedding a custom policy engine | Stub / Approximation | High | ✅ |
| AP27 | **Overview mixed information cards with configuration surfaces** — the overview dashboard rendered MCP connection setup, EDA settings guidance, OPA guardrail status/configuration, and an ROI assumptions editor alongside status cards; overview now keeps informational resource/job/project/inventory/ROI/insights/performance cards only and aligns the remaining cards into full/half-width rows | Implementation Bug | High | ✅ |
| AP28 | **Template create links logged false missing-route errors on first render** — Templates rendered create actions before `PageApp` populated the navigation route map, causing transient `Page id ... not found` console errors even though the links resolved after context initialization; `useGetPageUrl()` now waits until routes exist before reporting a missing ID, while still logging real missing routes afterward | Implementation Bug | Medium | ✅ |
| AP29 | **Overview insights crashed on malformed list payloads** — Automation Insights assumed every overview API response included a paginated `results` array; if auth/proxy/backend drift returned valid JSON without `results`, the overview crashed on `length`. The card now normalizes list payloads, shows an inline unavailable state on fetch errors, and keeps the overview page alive | Implementation Bug | High | ✅ |
| AP30 | **Workflow approval notifications lacked inline decisions** — Pending workflow approvals appeared in the masthead notification drawer but forced users to open the detail page to approve or deny; the drawer now renders Approve/Deny actions backed by the central `/workflow_approvals/{id}/approve|deny/` endpoints and uses the API count so pending approval badges stay correct beyond the visible page | Implementation Bug | High | ✅ |
| AP31 | **UI quality gate was blocked by accumulated lint drift** — Full `awx/ui/src` checks failed on stale Prettier formatting, unused imports, non-native click handlers, untranslated shortcut labels, a stale Terraform delete import, and one unstable memo dependency; the UI tree now passes `npm run test` so future slices get a reliable frontend regression gate | Environment / Setup Issue | High | ✅ |
| AP32 | **EDA live controller facade used non-upstream start/log paths** — The EDA facade claimed live controller parity but started activations through `/activations/{id}/start/` and read logs through `/activations/{id}/events/`; upstream `ansible/eda-server` exposes activation start as `/activations/{id}/enable/` and activation logs through `/activation-instances/{id}/logs/`, so live EDA start/event views could 404. The facade now defaults to upstream-compatible enable and activation-instance log paths, derives the latest/current activation instance, normalizes logs into AWX activation events, and keeps the old custom events path as a fallback | Implementation Bug | High | ✅ |
| AP33 | **AI live AWX answers ignored natural scope filters** — The assistant could answer broad visible-resource count/list questions without a provider call, but prompts such as "list hosts in inventory source-inv" still fell back to provider context or listed all visible rows. Direct AWX fact answers now detect visible inventory, organization, and status names in the prompt, apply the same RBAC-filtered queryset scope before counting/listing, and still avoid provider calls for scoped resource questions | Implementation Bug | High | ✅ |
| AP34 | **OPA decision parsing ignored common structured denials** — Real OPA responses commonly use `allow`, `deny`, `denied`, `violations`, or `errors` rather than only `allowed`; the guardrail parser now honors these decision shapes, lets explicit denial metadata override allow flags, treats undefined or unknown structured decisions as denied, and preserves fail-open only for the explicit disabled/unreachable engine paths | Implementation Bug | High | ✅ |
| AP35 | **AI live AWX answers over-listed related resources** — Direct assistant fact answers could count/list visible resources but only scoped by inventory, organization, and status, so prompts such as "list jobs for template X", "list job templates in project Y", or "list schedules for template Z" could return unrelated rows. The AWX grounding layer now detects visible related resource names for projects, job templates, unified job templates, credential types, source projects, and catalog items before answering without a provider call | Implementation Bug | High | ✅ |
| AP36 | **AI live AWX facts omitted non-playbook job classes and approvals** — Direct assistant count/list answers covered regular Jobs but not Workflow Jobs, Project Updates, Inventory Updates, System Jobs, Terraform Jobs, or Workflow Approvals, forcing generic provider answers for core AWX runtime objects. The live AWX grounding registry now includes these RBAC-filtered resources with status, organization, inventory/template/project scoping, and approval assignee/template context | Implementation Bug | High | ✅ |
| AP37 | **AI live AWX facts omitted audit and notification surfaces** — Direct assistant count/list answers could not answer about Activity Stream audit events, Notification Templates, or emitted Notifications, even though these are core operator/debugging surfaces. The AWX grounding registry now includes RBAC-filtered audit events, notification templates, and notification deliveries with actor, operation, status, organization, and template scoping while excluding notification secret configuration | Implementation Bug | High | ✅ |
| AP38 | **AI live AWX facts omitted EDA controller status and activations** — Direct assistant count/list/status answers only used AWX database resources, so EDA controller status and live rulebook activations still fell back to the external provider even though AWX has a native EDA facade. The assistant now answers EDA controller configured/invalid/unconfigured status plus live activation counts/lists through the EDA facade without provider calls, and reports controller unavailability explicitly | Implementation Bug | High | ✅ |
| AP39 | **AI live AWX facts omitted OPA guardrail status and audit outcomes** — Direct assistant answers could not report OPA enabled/configured state, registered policy paths, managed bundle status, or recent AI resource-action guardrail denials without falling back to the provider. The assistant now answers OPA status, policy path count/list, and bounded recent guardrail audit/denial history from AWX settings and Activity Stream while keeping the details system-admin only | Implementation Bug | High | ✅ |
| AP40 | **EDA live authentication and automation smoke proof were incomplete** — The AWX EDA facade only supported Bearer tokens, but the live upstream `ansible/eda-server` deployment is reachable with basic auth in this environment; `EDA_USERNAME` / encrypted `EDA_PASSWORD` settings now provide a token-preserving basic-auth fallback, and `awx-manage check_external_automation` verifies EDA activation reads plus OPA health/policy evaluation without exposing secrets | Implementation Bug | High | ✅ |
| AP41 | **OPA live policy sync was not covered by repeatable smoke tests** — The Policy as Code UI/API could PUT `OPA_POLICY_BUNDLE` to real OPA, but the live automation smoke only checked health and evaluation, so bundle sync regressions could pass parity checks. `awx-manage check_external_automation --sync-opa-policy` now validates policy ids, syncs the configured bundle through OPA's Policy API, reports checksum/line metadata, and then evaluates the smoke decision path | Implementation Bug | High | ✅ |
| AP42 | **EDA activation create/start smoke needed upstream schema parity** — AWX could list live EDA activations, but repeatable smoke did not prove activation creation/start and the facade still posted legacy `rulebook_name` payloads before resolving upstream `rulebook_id`, `decision_environment_id`, and `organization_id`. The facade now retries upstream-compatible activation creates after a 400, auto-resolves IDs from live EDA resources, and `awx-manage check_external_automation --start-eda-activation` can create/start/delete a live smoke activation | Implementation Bug | High | ✅ |
| AP43 | **OPA deny-path live smoke was missing** — OPA smoke proved health, policy sync, and allow decisions, but did not prove deny decisions or temp policy cleanup against live OPA. `awx-manage check_external_automation --opa-deny-smoke` now installs a temporary deny policy through OPA's Policy API, verifies a denied evaluation, deletes the temporary policy, and fails on unexpected allow or cleanup failure | Implementation Bug | High | ✅ |
| AP44 | **External automation smoke was CLI-only** — Live EDA/OPA parity checks were repeatable from `awx-manage` but invisible in the AWX UI/API, so admins could not verify controller/API wiring from the UI. `/api/v2/external_automation/check/` now exposes the same admin-only smoke runner; Settings → Event-Driven Ansible shows EDA+OPA smoke, while Policy as Code → Smoke Test shows OPA-only smoke with policy sync, allow evaluation, deny evaluation, temporary policy cleanup, and audit links | Implementation Bug | High | ✅ |
| AP45 | **External automation smoke lacked audit trail** — The Settings smoke runner could prove live EDA/OPA wiring but did not write Activity Stream evidence, so later operators could not see who ran the check or whether it failed. `/api/v2/external_automation/check/` now writes an `external_automation` Activity Stream entry with safe EDA/OPA status metadata for every run and returns the audit id, which the Settings smoke panel displays after completion | Implementation Bug | High | ✅ |
| AP46 | **External automation audit events were hard to find** — Activity Stream had no type filter for the new `external_automation` audit rows, the Settings smoke panel showed an id without navigation, and actorless system rows could crash Activity Stream when large pages included them. Activity Stream now includes an External Automation type filter, external automation rows render as smoke check events, actorless rows render as system, and the Settings smoke result links directly to the exact Activity Stream audit row | Implementation Bug | High | ✅ |
| AP47 | **Approval notifications still jumped out of the notification approval surface** — Masthead workflow approval notifications had inline Approve/Deny, but clicking the notification row navigated to the approval detail page instead of the central approvals list, so operators were pulled back into a detail/template-style path. Notification approval rows now route to Workflow Approvals filtered by approval id and pending status, and the Workflow Approvals list exposes a Status filter so pending approval links stay grounded in the central approval section | Implementation Bug | High | ✅ |
| AP48 | **AI live AWX facts omitted cloud provider states** — Org-scoped `CloudProviderState` records backed Marketplace and inventory mapping, but the AI live grounding registry only exposed cloud connections, so assistant questions about pulled provider state still fell through to generic provider answers. The assistant now counts/lists RBAC-filtered provider state rows, supports organization scoping, includes state metadata in authoring context, and keeps raw provider payloads out of direct answers | Implementation Bug | High | ✅ |
| AP49 | **AI live AWX facts stopped at provider-state metadata** — The assistant could report that pulled cloud state rows existed, but still could not answer user-visible questions such as "how many DigitalOcean images?" or "list Proxmox VMs for this connection" from the org-scoped provider payloads that power Marketplace and inventory mapping. The assistant now counts/lists safe pulled cloud resources from DigitalOcean, Proxmox, VMware, and Azure provider state, supports provider/organization/connection scoping, and keeps credential-like keys filtered through the existing cloud-inventory sanitizer | Implementation Bug | High | ✅ |
| AP50 | **AI project-file rollback plans did not restore overwritten content** — Existing manual project file update/delete operations had private failure rollback but returned/audited rollback plans omitted the previous file content, so an approved AI playbook/role edit could not be restored from the Activity Stream rollback plan. Text project-file updates now emit an applicable `project_file` restore operation with the previous UTF-8 content, while oversized/binary/null-byte files still report an explicit unsupported reason | Implementation Bug | High | ✅ |
| AP51 | **OPA management is not Gatekeeper Policy Manager parity** — Plain OPA live module management is covered by G6d, the Gatekeeper inventory/report/drill-down/context selector view with URL-backed context/detail/filter/sort/page/per-page permalinks and true paginated violation tables is covered by G6e, and governed Gatekeeper preview/dry-run/apply/delete/rollback/server-side apply plus AI-assisted manifest authoring is covered by G6f | Stub / Approximation | High | ✅ |
| AP52 | **AI live AWX facts omitted Gatekeeper policy-manager state** — Direct assistant answers covered OPA paths/audits but not live Gatekeeper ConstraintTemplates, constraints, configs, violations, or connection status, so Gatekeeper questions could still fall back to generic provider output. The assistant now answers Gatekeeper status/count/list questions through the same Gatekeeper facade used by Policy as Code, keeps secrets out of responses, routes OPA Gatekeeper prompts before generic OPA handling, and keeps details system-admin only | Implementation Bug | High | ✅ |
| AP53 | **Gatekeeper selected resources were not wired to governed delete** — The Gatekeeper UI could drill into templates, constraints, and configs, while the delete API already supported target-based deletion, but the UI only posted the manifest editor content. Operators now can explicitly use the selected Gatekeeper resource as the delete target, see whether delete uses the selected resource or manifest textarea, and still get preview/dry-run/delete plus rollback plan behavior through the governed delete endpoint | Implementation Bug | High | ✅ |
| AP54 | **Gatekeeper governed actions hid Activity Stream evidence** — Gatekeeper author/apply/delete/rollback responses returned Activity Stream audit IDs and URLs, but the UI only exposed them inside raw JSON blocks. The governed changes panel now renders direct Activity Stream links for AI authoring, apply, delete, and rollback results so operators can jump from a policy action to its audit event | Implementation Bug | Medium | ✅ |
| AP55 | **Plain OPA module actions hid Activity Stream evidence** — Policy as Code → Policy Modules stored create/update/delete/rollback Activity Stream records and version history IDs, but operators had to inspect raw API payloads or remember numeric IDs. Module action success states and selected version history now render direct Activity Stream links, including rollback source snapshots and newly written rollback audit events | Implementation Bug | Medium | ✅ |
| AP56 | **Plain OPA module delete had no confirmation** — Policy as Code → Policy Modules deleted live OPA modules immediately from the toolbar button, even though the action changes enforcement behavior. Delete now opens a danger confirmation dialog, shows the exact policy ID, preserves cancel as a no-op, and only sends the DELETE request after explicit confirmation | Implementation Bug | High | ✅ |
| AP57 | **OPA module editor auto-reselected stale modules** — The Policy Modules page auto-selected the first live module whenever `selectedModuleId` became empty, so clicking `New module` or deleting a module could immediately reload a stale module list and clear the user's create/delete result state. Auto-select now runs only on initial load; manual New, Delete, Save, and dropdown selection preserve intended editor/result state | Implementation Bug | Medium | ✅ |
| AP58 | **Gatekeeper live writes lacked final confirmation** — Policy as Code → Gatekeeper guarded writes used preview/dry-run/apply modes, but live apply, delete, and rollback buttons could send Kubernetes-changing requests immediately after mode selection. Live Gatekeeper apply/delete/rollback now open a danger confirmation dialog with context/target details, cancel is a no-op, and requests are sent only after explicit confirmation | Implementation Bug | High | ✅ |
| AP59 | **Policy smoke skipped Gatekeeper connection proof** — Policy Connections carried Gatekeeper Kubernetes API settings, but shared external automation smoke only checked EDA/OPA, so admins could save Gatekeeper config and still lack one repeatable read-only proof. The smoke runner now supports a Gatekeeper check that reads configured context, ConstraintTemplates, constraints, configs, and violation counts; Settings → Policy Connections and Policy as Code → Smoke Test run OPA plus Gatekeeper checks and show Gatekeeper status/counts/audit evidence | Implementation Bug | High | ✅ |
| AP60 | **Policy smoke ignored named Gatekeeper contexts** — The external automation API accepted a Gatekeeper context name and Gatekeeper Policy Manager supported named context selection, but Policy Smoke always sent an empty context so admins could not validate non-default clusters from the UI. Policy Smoke now exposes an optional Gatekeeper context input and sends the trimmed value with the smoke request | Implementation Bug | Medium | ✅ |
| AP61 | **Gatekeeper unconfigured state was a dead-end** — Policy as Code → Gatekeeper told admins to configure the Kubernetes API in Settings but did not provide a route to the Policy Connections page, leaving the setup path implicit. The empty-state warning now links directly to Settings → Policy Connections so Gatekeeper read/write setup stays wired from the failing surface | Implementation Bug | Medium | ✅ |
| AP62 | **Policy smoke could not isolate OPA and Gatekeeper checks** — The API already accepted independent OPA and Gatekeeper smoke flags, but Settings → Policy Connections and Policy as Code → Smoke Test always ran both together, so a broken/unconfigured OPA service could block validating Gatekeeper-only Kubernetes connectivity. Policy Smoke now exposes OPA/Gatekeeper checkboxes, blocks empty selections, sends exact include flags, hides irrelevant result rows, and keeps named Gatekeeper context input tied to the selected Gatekeeper check | Implementation Bug | Medium | ✅ |
| AP63 | **Policy pages were width-capped and form rows looked misaligned on wide displays** — Policy as Code pages reused width-limited PageSections and mixed unlabeled search inputs with labeled selects, so 32-inch/wide screens showed unused horizontal space and crooked form rows in Gatekeeper governed changes and violations. Policy overview/modules/tester/smoke/Gatekeeper sections now use full-width PageSections, Gatekeeper control grids use responsive breakpoints, action buttons align to their related controls, summary cards expand cleanly, and violation search has a real label aligned with sort/per-page controls | Implementation Bug | Medium | ✅ |
| AP64 | **Live Proxmox proof is still missing** — Provision or confirm dedicated Proxmox VMs for EDA Server, OPA/Gatekeeper, and Gatekeeper Policy Manager; install/configure services; wire AWX Settings → Event-Driven Ansible and Settings → Policy Connections to the live endpoints; then run real EDA/OPA/Gatekeeper smoke from the UI/API and record evidence, URLs, service status, and any failures. Progress: Policy Smoke now exports a downloadable evidence JSON report with requested checks, result payload, audit link data, and timestamp; current Proxmox API preflight from this Codex machine is blocked by network reachability (profile endpoint timed out, public host returned no route) | Environment / Live Proof | High | 🔄 |
| AP65 | **Completed parity rows need a full live click audit** — Re-check every marked-done feature across UI, API, and docs; use live UI navigation plus focused backend/frontend tests; demote any broken or partially wired row back to incomplete with exact evidence instead of assuming ✅ rows are still true | Parity Audit | High | ⬜ |
| AP66 | **Wide/mobile UI polish pass is still open** — Validate Policy as Code, AI assistant/resource action surfaces, Catalog, Cloud, and related Settings pages on wide desktop and mobile widths; fix unused whitespace, misaligned controls, overflow, text overlap, dead links, and inconsistent responsive behavior | UI Polish | Medium | ⬜ |
| AP67 | **AI end-to-end resource creation proof is complete** — From the live AWX UI assistant, a pasted reviewed plan created and previewed one combined bundle for inventory, group, host, smart inventory, manual project workspace, playbook file, role task/default files, and launch-wired job template; apply produced Activity Stream audit `#666`, object/detail links, project-file links, and a job-template launch link; rollback produced Activity Stream audit `#671`, deleted the DB resources/files, and retained audit evidence. Backend AP78 still covers the same bundle deterministically in pytest | Live Proof / AI | High | ✅ |
| AP68 | **Dev admin password drift blocked live UI proof** — Docker Compose bootstrap created the `admin` user only when missing and then printed `DJANGO_SUPERUSER_PASSWORD` even if an existing admin hash no longer matched that value, so repeatable browser smoke checks could get stuck at login. The bootstrap now synchronizes the existing admin user email/superuser/staff/active flags and password hash to `DJANGO_SUPERUSER_PASSWORD` every run, preserving the printed dev credential as a real login path | Environment / Setup Issue | High | ✅ |
| AP69 | **Gatekeeper smoke failure was a setup dead-end** — Policy as Code → Smoke Test and Settings → Policy Connections could report Gatekeeper `not_configured`, but the smoke result did not route operators back to the Policy Connections settings needed to fix the Kubernetes API URL/token/context. Failed Gatekeeper smoke now renders an inline Policy Connections link while preserving the audit/evidence payload, and Cypress covers the unconfigured smoke path | Implementation Bug | Medium | ✅ |
| AP70 | **AI assistant answer formatting lacked regression proof** — Earlier live use showed assistant answers with raw Markdown markers instead of readable bullets/emphasis/code. The assistant already renders responses through `react-markdown`, and AP65 audit now locks that behavior with stable message test IDs plus a Cypress test proving host-list style answers render as formatted `<strong>`, list items, and inline code instead of raw `**` or dash-prefixed Markdown text | Parity Audit / Test Coverage | Medium | ✅ |
| AP71 | **AI resource apply hid Activity Stream evidence** — `/api/v2/ai/resource_actions/` apply responses can return an Activity Stream audit id, but the global AI assistant only showed applied object JSON and rollback JSON, so operators could not jump from an approved AI change to its audit event. Applied AI resource plans now render a direct Activity Stream link beside the plan result while preserving rollback details, and Cypress covers preview → apply → audit-link rendering | Implementation Bug | Medium | ✅ |
| AP72 | **AI rollback plans were not actionable from the assistant** — The backend already allowed returned rollback plans to be applied through `/api/v2/ai/resource_actions/`, but the global AI assistant only showed rollback JSON, leaving users to manually copy plans back into the API. Applied AI resource plans now expose an explicit Apply rollback action that posts the audited rollback plan with rollback context, replaces the result with the rollback apply response, and keeps Activity Stream audit navigation visible | Implementation Bug | High | ✅ |
| AP73 | **AI-created resource results were not navigable** — AP67 requires users to verify AI-created inventories, projects, and templates from the UI, but the assistant only showed returned JSON/object IDs after apply. Valid applied AI operations now render direct detail links for inventories, smart/constructed inventories, projects, job templates, workflow job templates, hosts, and catalog items, while delete rollback results stay non-linked to avoid dead routes | Implementation Bug | Medium | ✅ |
| AP74 | **AI-created templates had no launch shortcut** — AP67 requires proving that AI-created Job Templates and Workflow Job Templates are launch-wired, but the assistant only linked to template details after apply. Applied AI job-template and workflow-template operations now render direct launch links to the existing launch wizards, with Cypress coverage for both launch routes | Implementation Bug | Medium | ✅ |
| AP75 | **Smart/constructed AI inventory proof was raw JSON only** — The backend returns smart inventory matched-host/group previews and constructed inventory input/source previews, but the assistant forced users to inspect JSON to verify AI inventory scope before/after apply. Smart and constructed inventory operation cards now render readable preview summaries with counts and sample names while preserving raw JSON, and Cypress covers smart/constructed detail routes plus preview summary text | Implementation Bug | Medium | ✅ |
| AP76 | **AI-authored playbook/role file results were raw JSON only** — AP67 requires users to verify AI-created playbooks and roles from the UI, but `project_file` operations only exposed project/path/content bytes inside JSON. AI project-file operations now render a direct Project link labeled with the file path plus readable file/content-byte summaries and project-file preview change metadata, while preserving raw JSON for audit/debug detail | Implementation Bug | Medium | ✅ |
| AP77 | **OpenAI Codex device chat used stale unsupported models** — Settings/model refresh could cache or default to generic GPT/old Codex model IDs such as `gpt-5.2` that ChatGPT-backed Codex rejects, causing `/api/v2/ai/chat/` to return 502 Bad Gateway. Codex device model options now filter to live-supported ChatGPT Codex models, default to `gpt-5.5` with `gpt-5.4` fallback, ignore stale cached/default values, and map unsupported requested models to the safe default before provider calls | Implementation Bug | High | ✅ |
| AP78 | **AI authoring parity lacked one full backend bundle proof** — Individual tests covered inventory trees, smart inventories, project files, new Project workspaces, job templates, Activity Stream audit, and rollback separately, but AP67 still lacked one deterministic test proving the full playbook/role/inventory/smart-inventory/job-template slice together. The functional API suite now applies one combined AI resource plan with same-plan inventory/group/host/project refs, writes playbook plus role files, creates a launch-wired Job Template, verifies Activity Stream relations and smart-inventory preview, then applies the returned rollback plan and proves every created record/file is removed | Live Proof / AI | High | ✅ |
| AP79 | **AI rollback delete could leave stale Activity Stream resource links** — Applying an AI rollback plan that deleted a Project after related project-file deletes could leave Activity Stream many-to-many rows pointing at the deleted Project, tripping database constraints in the full AP78 rollback proof. AI delete operations now clear existing Activity Stream relation rows before deleting the target object, and project-file audit linking skips already-deleted Projects while preserving rollback evidence in the audit JSON | Implementation Bug | High | ✅ |
| AP80 | **AI assistant live apply proof depended on provider-generated plan text** — The assistant's Plan changes button always sent input as a prompt, so AP67 live UI proof depended on the model producing the exact multi-resource JSON plan instead of letting operators paste a reviewed plan and still use the same preview/apply/RBAC/OPA/audit path. The assistant now detects pasted JSON plan objects with `operations`, sends them as `plan` to `/api/v2/ai/resource_actions/` for preview, and falls back to prompt generation for normal natural-language input; Cypress proves pasted plans omit `prompt` and render the preview | Implementation Bug | High | ✅ |
| AP81 | **Overview cards could clip text and leave uneven paired-card space** — Dashboard card `height` incorrectly also applied a `max-height`, so taller overview content could spill outside its card instead of growing the grid row. `PageDashboardCard` now treats `height` as a minimum/grid span and only clamps when explicit `maxHeight` is set, Automation ROI uses the same row span as Automation Insights, and Insights text wraps long job/template names inside the card | UI Polish / Implementation Bug | Medium | ✅ |
| AP82 | **AI rollback results could show dead project-file links** — Live AP67 proof showed rollback `project_file` delete operations rendering "View project for deleted file" links even when the same rollback deleted the parent Project later, creating dead UI links after cleanup. Project-file delete operations now suppress project detail links while preserving file summaries, raw rollback evidence, and Activity Stream links; Cypress covers the delete-file rollback case | Implementation Bug | High | ✅ |
| AP83 | **AI assistant panel needed responsive polish** — The global assistant still used a narrow fixed 640px shell and sat hard against the mobile viewport edge, making AI/resource-plan review cramped on wide displays and awkward on small screens. The panel now uses a 720px desktop shell, mobile-safe insets, a scroll-contained body, wrapping action rows, overflow-safe message bubbles, and Cypress viewport checks for desktop and mobile geometry | UI Polish / Implementation Bug | Medium | ✅ |
| AP84 | **Cloud provider overviews and catalog provider details needed responsive polish** — Cloud provider overview tabs reused a centered 1400px grid, wasting wide-screen space and making admin panels feel narrower than the rest of the app; Catalog Item provider workflow details used a fixed two-column grid that squeezed labels on mobile. Cloud overviews now share a full-width responsive section/grid with stable data hooks, while Catalog provider workflow details collapse to one column below mobile/tablet widths | UI Polish / Implementation Bug | Medium | ✅ |
| AP85 | **OpenAI Codex settings could overflow on mobile** — The AI Assistant settings page used raw flex rows plus unbounded description-list content for Codex account IDs, model names, device codes, and verification URLs, so long values could create horizontal scroll or squeezed buttons on small screens. The Codex device-login section now wraps action rows, constrains clipboard/select content, breaks long account/model/URL text safely, exposes stable data hooks, and has Cypress mobile viewport coverage for the started device-login flow | UI Polish / Implementation Bug | Medium | ✅ |
| AP86 | **Host facts were not visible as an operational dashboard** — Gathered facts stayed in `Host.ansible_facts` and the Hosts list opened Details, so operators could not click a host into a server dashboard and facts were not mirrored into host vars for normal hostvar use. Fact-cache completion now syncs current facts into `host.variables.ansible_facts` while preserving existing vars and removing stale mirrored facts on clear; standalone, inventory, smart, constructed, and group host list clicks now open a Host Dashboard tab with system, compute, storage, network, runtime, and recent automation cards backed by `/hosts/{id}/ansible_facts/` | Implementation Bug / UI Enhancement | High | ✅ |
| AP87 | **Gather-facts jobs used remote home temp directories** — Ad hoc `setup`/gather-facts and playbook jobs could fail on locked-down images when Ansible tried to create `~/.ansible/tmp` under a rejected login home such as root. Playbook and ad hoc runner environments now default both supported Ansible remote temp env spellings to `/tmp/ansible`, mirror explicit AWX env overrides, preserve project `ansible.cfg` `remote_tmp` when present, and Terraform-populated Proxmox hosts now carry `ansible_user` from `cloud_init_user` plus `ansible_remote_tmp` for downstream jobs | Implementation Bug | High | ✅ |
| AP88 | **Ad hoc setup did not persist host facts** — Running gather facts through an inventory/host command captured setup output but skipped durable host fact persistence, so host facts and mirrored `host.variables.ansible_facts` stayed stale. Ad hoc `setup` now enables AWX fact-cache wiring and persists `runner_on_ok` setup event facts after callback wrapup; the Host Dashboard has a Pull facts button that launches host-scoped setup and opens the command output job | Implementation Bug / UI Enhancement | High | ✅ |
| AP89 | **Host dashboard Pull facts navigated away from the dashboard** — Pull facts immediately opened the command output page, so the host dashboard did not show in-place progress or completion. The button now stays on the dashboard, shows Pulling facts while polling the setup job, refreshes facts after success, briefly shows a checkmarked Facts updated state, then resets to Pull facts | UI Polish / Implementation Bug | Medium | ✅ |
| AP90 | **Host dashboard omitted useful gathered facts and collection time** — Setup facts include timestamp, identity, security, firmware, block-device, DNS, interface, load-average, and user-context data that were not visible on the dashboard. The Host Dashboard now shows last facts pull time plus compact Identity, Security, Hardware, Network interfaces, and Block devices cards with wrapping-safe values and Cypress coverage | UI Enhancement | Medium | ✅ |
| AP91 | **Inventory-level default machine credential policy** — Inventories can now define an optional default Machine credential plus a force toggle. Job launches using that inventory fill in the inventory credential only when the template/prompt lacks a Machine credential, or replace any template/prompt Machine credential when force is enabled, while preserving cloud/vault/network/Kubernetes credentials. The API validates Machine-only credentials, same-org scope, force-with-credential requirements, credential `use` permission, and launch-time password prompts; the inventory form/details UI exposes the setting with Cypress and backend coverage | UI Enhancement / Backend Behavior | High | ✅ |
| AP92 | **Host Pull facts ignored inventory credential policy** — Host, group, and inventory ad hoc command launches did not resolve the inventory default Machine credential policy, so Pull facts and other ad hoc setup runs could keep a submitted credential even when force was enabled or fail before the inventory default credential could be applied. Ad hoc command creation now resolves the inventory default/force credential before password checks, serializer validation, RBAC checks, job creation, and job start | Implementation Bug | High | ✅ |
| AP93 | **Job output AI lacked run and code context** — The job output page could open generic AI chat without sending the viewed job identity, output, diagnostic events, or project code, so failure and improvement questions could get generic answers. Job Output now has a toolbar AI action that sends page/job context, `/api/v2/ai/chat/` resolves the job through RBAC, injects redacted output/error events and safe related project code snippets into the provider prompt, and Cypress/API tests cover context handoff plus prompt grounding | UI Enhancement / Implementation Bug | High | ✅ |

## Notes

- **Build command**: `bash tools/scripts/deploy-ui.sh` from repo root
- **Container**: `tools_awx_1`, source bind-mounted at `/awx_devel/`
- **UI source**: `awx/ui/src/`
- **Framework**: React + PatternFly 5 + TypeScript
- **API**: DRF at `/api/v2/`
